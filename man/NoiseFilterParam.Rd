% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanGenome.R
\name{NoiseFilterParam}
\alias{NoiseFilterParam}
\title{Create Filtering Parameters for SNR estimation}
\usage{
NoiseFilterParam(
  mean_probs = c(0.05, 0.99),
  depth_probs = c(0.01, 0.95),
  noise_ratio_probs = c(0.01, 0.7),
  dcut_min = 10,
  na.rm = TRUE
)
}
\arguments{
\item{mean_probs}{Numeric length-2 vector. Lower and upper quantile
cutoffs for the mean methylation values. Default \code{c(0.05, 0.99)}.}

\item{depth_probs}{Numeric length-2 vector. Lower and upper quantile
cutoffs for the coverage values. Default \code{c(0.01, 0.95)}.}

\item{noise_ratio_probs}{Numeric length-2 vector. Lower and upper
quantile cutoffs for the noise-to-mean ratio. Default
\code{c(0.01, 0.70)}.}

\item{dcut_min}{Numeric scalar. Minimum allowed depth cutoff
(floor). Default \code{10}.}

\item{na.rm}{Logical; should \code{NA} values be removed before
computing quantiles? Default \code{TRUE}.}
}
\value{
An object of class \code{"NoiseFilterParam"}, which is just a
list with class attribute and a custom print method.
}
\description{
Constructor for a simple parameter list controlling
thresholds used in \code{estimateNoiseParsWindows()} to filter
windows based on mean methylation, coverage, and noise ratio.
}
\seealso{
\code{\link{estimateNoiseParsWindows}}
}
