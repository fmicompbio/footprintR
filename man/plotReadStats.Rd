% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotReadStats.R
\name{plotReadStats}
\alias{plotReadStats}
\title{Plot distribution of QC statistics}
\usage{
plotReadStats(
  se,
  readInfoCol = "readInfo",
  qcCol = "QC",
  minQscore = NULL,
  maxEntropy = NULL,
  maxFracLowConf = NULL,
  minReadLength = NULL,
  minAlignedLength = NULL,
  minAlignedFraction = NULL,
  minSNR = NULL
)
}
\arguments{
\item{se}{A \code{\link[SummarizedExperiment]{RangedSummarizedExperiment}},
typically generated by \code{addReadStats}.}

\item{readInfoCol}{A character scalar providing the name of the column in
\code{colData} that contains read info. Can be \code{NULL} if no such
column exists.}

\item{qcCol}{A character scalar providing the name of the column in
\code{colData} that contains quality metrics (calculated by
\code{calcReadStats}). Can be \code{NULL} if no such column exists.}

\item{minQscore, maxEntropy, maxFracLowConf, minReadLength, minAlignedLength, minAlignedFraction, minSNR}{Numeric scalars representing possible threshold values used in
\code{\link{filterReads}}, for illustration in the plot panels.}
}
\value{
A \code{ggplot} object.
}
\description{
This function plots histograms of the various per-read summary statistics
calculated by \code{\link{addReadStats}}.
}
\examples{
library(SummarizedExperiment)
modbamfiles <- system.file("extdata",
                           c("6mA_1_10reads.bam", "6mA_2_10reads.bam"),
                          package = "footprintR")
se <- readModBam(bamfile = modbamfiles, regions = "chr1:6940000-6955000",
                 modbase = "a", verbose = TRUE,
                 BPPARAM = BiocParallel::SerialParam())
se <- addReadStats(se, BPPARAM = BiocParallel::SerialParam())
plotReadStats(se)

}
\author{
Panagiotis Papapasaikas, Charlotte Soneson, Michael Stadler
}
